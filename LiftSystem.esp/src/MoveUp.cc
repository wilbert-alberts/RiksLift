// Generated by dzn code from MoveUp.dzn
#include "MoveUp.hh"
#include <dzn/locator.hh>
#include <dzn/runtime.hh>
#include <iterator>
#define STRINGIZING(x) #x
#define STR(x) STRINGIZING (x)
#define LOCATION __FILE__ ":" STR (__LINE__)
MoveUp::MoveUp (dzn::locator const& locator)
: dzn_meta ({"MoveUp","MoveUp",0,  {},  {},  {[this] ()
        {
          p.dzn_check_bindings ();
        }, [this] ()
        {
          timer.dzn_check_bindings ();
        }, [this] ()
        {
          fastDestinationSensor.dzn_check_bindings ();
        }, [this] ()
        {
          slowDestinationSensor.dzn_check_bindings ();
        }, [this] ()
        {
          positionSensor.dzn_check_bindings ();
        }, [this] ()
        {
          motor.dzn_check_bindings ();
        }, [this] ()
        {
          planner.dzn_check_bindings ();
        }}})
, dzn_runtime (locator.get<dzn::runtime> ())
, dzn_locator (locator)
, state (::MoveUp::State::IDLE)
, p ({  {"p",&p,this,&dzn_meta},  {"p",0,0,0}},this)
, timer ({  {"timer",0,0,0},  {"timer",&timer,this,&dzn_meta}},this)
, fastDestinationSensor ({  {"fastDestinationSensor",0,0,0},  {"fastDestinationSensor",&fastDestinationSensor,this,&dzn_meta}},this)
, slowDestinationSensor ({  {"slowDestinationSensor",0,0,0},  {"slowDestinationSensor",&slowDestinationSensor,this,&dzn_meta}},this)
, positionSensor ({  {"positionSensor",0,0,0},  {"positionSensor",&positionSensor,this,&dzn_meta}},this)
, motor ({  {"motor",0,0,0},  {"motor",&motor,this,&dzn_meta}},this)
, planner ({  {"planner",0,0,0},  {"planner",&planner,this,&dzn_meta}},this)
{
  this->dzn_meta.require =   {&timer.dzn_meta,&fastDestinationSensor.dzn_meta,&slowDestinationSensor.dzn_meta,&positionSensor.dzn_meta,&motor.dzn_meta,&planner.dzn_meta};
  this->dzn_runtime.performs_flush (this) = true;
  this->p.in.moveUp = [this] ()
    {
      this->dzn_out_p = &this->p.in.moveUp.dzn_out_binding;
      this->p_moveUp ();
    };
  this->p.in.abort = [this] ()
    {
      this->dzn_out_p = &this->p.in.abort.dzn_out_binding;
      this->p_abort ();
    };
  this->timer.out.timerElapsed = [this] ()
    {
      this->timer_timerElapsed ();
    };
  this->fastDestinationSensor.out.destinationReached = [this] ()
    {
      this->fastDestinationSensor_destinationReached ();
    };
  this->slowDestinationSensor.out.destinationReached = [this] ()
    {
      this->slowDestinationSensor_destinationReached ();
    };
}
void
MoveUp::p_moveUp ()
{
  if (this->state == ::MoveUp::State::IDLE)
    {
      this->positionSensor.in.getCurrentPosition (this->currentPosition);
      this->planner.in.getMoveUpFastPlanning (this->currentPosition, this->fastDelay, this->fastPosition);
      this->planner.in.getMoveUpSlowPlanning (this->currentPosition, this->slowDelay, this->slowPosition);
      this->timer.in.setTimer (fastDelay.plus(slowDelay));
      this->fastDestinationSensor.in.setDestination (this->fastPosition);
      this->slowDestinationSensor.in.setDestination (this->slowPosition);
      this->motor.in.moveUpFast ();
      this->state = ::MoveUp::State::MOVING_FAST;
    }
  else if (!(this->state == ::MoveUp::State::IDLE)) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
MoveUp::p_abort ()
{
  if (this->state == ::MoveUp::State::MOVING_FAST)
    {
      this->timer.in.cancelTimer ();
      this->fastDestinationSensor.in.cancelDestination ();
      this->slowDestinationSensor.in.cancelDestination ();
      this->motor.in.stop ();
      this->timer.in.setTimer (Delay(TIMEOUT_ABORT_WHILE_MOVING_FAST));
      this->state = ::MoveUp::State::ABORTING;
    }
  else if (this->state == ::MoveUp::State::MOVING_SLOW)
    {
      this->timer.in.cancelTimer ();
      this->slowDestinationSensor.in.cancelDestination ();
      this->motor.in.stop ();
      this->timer.in.setTimer (Delay(TIMEOUT_ABORT_WHILE_MOVING_SLOW));
      this->state = ::MoveUp::State::ABORTING;
    }
  else if (this->state == ::MoveUp::State::STOPPING)
    {
      this->state = ::MoveUp::State::ABORTING;
    }
  else if (this->state == ::MoveUp::State::FAILING_STOPPING)
    {
      this->state = ::MoveUp::State::ABORTING_STOPPING;
    }
  else if (!(this->state == ::MoveUp::State::FAILING_STOPPING) && (!(this->state == ::MoveUp::State::STOPPING) && (!(this->state == ::MoveUp::State::MOVING_SLOW) && !(this->state == ::MoveUp::State::MOVING_FAST)))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
MoveUp::timer_timerElapsed ()
{
  if (this->state == ::MoveUp::State::STOPPING)
    {
      this->p.out.moveFinished ();
      this->state = ::MoveUp::State::IDLE;
    }
  else if (this->state == ::MoveUp::State::ABORTING)
    {
      this->p.out.moveAborted ();
      this->state = ::MoveUp::State::IDLE;
    }
  else if (this->state == ::MoveUp::State::MOVING_FAST)
    {
      this->motor.in.stop ();
      this->fastDestinationSensor.in.cancelDestination ();
      this->slowDestinationSensor.in.cancelDestination ();
      this->timer.in.setTimer (Delay(TIMEOUT_STOPPING_AFTER_FAILING));
      this->state = ::MoveUp::State::FAILING_STOPPING;
    }
  else if (this->state == ::MoveUp::State::MOVING_SLOW)
    {
      this->motor.in.stop ();
      this->slowDestinationSensor.in.cancelDestination ();
      this->timer.in.setTimer (Delay(TIMEOUT_STOPPING_AFTER_FAILING));
      this->state = ::MoveUp::State::FAILING_STOPPING;
    }
  else if (this->state == ::MoveUp::State::FAILING_STOPPING)
    {
      this->p.out.moveFailed ();
      this->state = ::MoveUp::State::IDLE;
    }
  else if (this->state == ::MoveUp::State::ABORTING_STOPPING)
    {
      this->p.out.moveAborted ();
      this->state = ::MoveUp::State::IDLE;
    }
  else if (!(this->state == ::MoveUp::State::ABORTING_STOPPING) && (!(this->state == ::MoveUp::State::FAILING_STOPPING) && (!(this->state == ::MoveUp::State::MOVING_SLOW) && (!(this->state == ::MoveUp::State::MOVING_FAST) && (!(this->state == ::MoveUp::State::ABORTING) && !(this->state == ::MoveUp::State::STOPPING)))))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
MoveUp::fastDestinationSensor_destinationReached ()
{
  if (this->state == ::MoveUp::State::MOVING_FAST)
    {
      this->motor.in.moveUpSlow ();
      this->state = ::MoveUp::State::MOVING_SLOW;
    }
  else if (!(this->state == ::MoveUp::State::MOVING_FAST)) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
MoveUp::slowDestinationSensor_destinationReached ()
{
  if (this->state == ::MoveUp::State::MOVING_SLOW)
    {
      this->motor.in.stop ();
      this->timer.in.cancelTimer ();
      this->timer.in.setTimer (TIMEOUT_STOP_WHILE_MOVING_SLOW);
      this->state = ::MoveUp::State::STOPPING;
    }
  else if (this->state == ::MoveUp::State::MOVING_FAST)
    {
      this->motor.in.stop ();
      this->timer.in.cancelTimer ();
      this->fastDestinationSensor.in.cancelDestination ();
      this->timer.in.setTimer (Delay(TIMEOUT_STOPPING_AFTER_FAILING));
      this->state = ::MoveUp::State::FAILING_STOPPING;
    }
  else if (!(this->state == ::MoveUp::State::MOVING_FAST) && !(this->state == ::MoveUp::State::MOVING_SLOW)) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
// version 2.18.2
