// Generated by dzn code from StatefullMoveUp.dzn
#include "StatefullMoveUp.hh"
#include <dzn/locator.hh>
#include <dzn/runtime.hh>
#include <iterator>
#define STRINGIZING(x) #x
#define STR(x) STRINGIZING (x)
#define LOCATION __FILE__ ":" STR (__LINE__)
SMoveUp::SMoveUp (dzn::locator const& locator)
: dzn_meta ({"SMoveUp","SMoveUp",0,  {},  {},  {[this] ()
        {
          p.dzn_check_bindings ();
        }, [this] ()
        {
          homedMove.dzn_check_bindings ();
        }, [this] ()
        {
          regularMove.dzn_check_bindings ();
        }}})
, dzn_runtime (locator.get<dzn::runtime> ())
, dzn_locator (locator)
, homedState (::SMoveUp::HomedState::NOT_HOMED)
, moveState (::SMoveUp::MoveState::IDLE)
, p ({  {"p",&p,this,&dzn_meta},  {"p",0,0,0}},this)
, homedMove ({  {"homedMove",0,0,0},  {"homedMove",&homedMove,this,&dzn_meta}},this)
, regularMove ({  {"regularMove",0,0,0},  {"regularMove",&regularMove,this,&dzn_meta}},this)
{
  this->dzn_meta.require =   {&homedMove.dzn_meta,&regularMove.dzn_meta};
  this->dzn_runtime.performs_flush (this) = true;
  this->p.in.moveUp = [this] ()
    {
      this->dzn_out_p = &this->p.in.moveUp.dzn_out_binding;
      this->p_moveUp ();
    };
  this->p.in.abort = [this] ()
    {
      this->dzn_out_p = &this->p.in.abort.dzn_out_binding;
      this->p_abort ();
    };
  this->homedMove.out.moveFinished = [this] ()
    {
      this->homedMove_moveFinished ();
    };
  this->homedMove.out.moveAborted = [this] ()
    {
      this->homedMove_moveAborted ();
    };
  this->homedMove.out.moveFailed = [this] ()
    {
      this->homedMove_moveFailed ();
    };
  this->regularMove.out.moveFinished = [this] ()
    {
      this->regularMove_moveFinished ();
    };
  this->regularMove.out.moveAborted = [this] ()
    {
      this->regularMove_moveAborted ();
    };
  this->regularMove.out.moveFailed = [this] ()
    {
      this->regularMove_moveFailed ();
    };
}
void
SMoveUp::p_moveUp ()
{
  if ((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::IDLE))
    {
      this->homedMove.in.moveUp ();
      this->moveState = ::SMoveUp::MoveState::MOVING;
    }
  else if ((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::IDLE))
    {
      this->regularMove.in.moveUp ();
      this->moveState = ::SMoveUp::MoveState::MOVING;
    }
  else if (!((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::IDLE)) && !((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::IDLE))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
SMoveUp::p_abort ()
{
  if ((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))
    {
      this->homedMove.in.abort ();
      this->moveState = ::SMoveUp::MoveState::ABORTING;
    }
  else if ((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))
    {
      this->regularMove.in.abort ();
      this->moveState = ::SMoveUp::MoveState::ABORTING;
    }
  else if (!((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING)) && !((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
SMoveUp::homedMove_moveFinished ()
{
  if ((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))
    {
      this->p.out.moveFinished ();
      this->moveState = ::SMoveUp::MoveState::IDLE;
      this->homedState = ::SMoveUp::HomedState::HOMED;
    }
  else if (!((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
SMoveUp::homedMove_moveAborted ()
{
  if ((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::ABORTING))
    {
      this->p.out.moveAborted ();
      this->moveState = ::SMoveUp::MoveState::IDLE;
    }
  else if (!((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::ABORTING))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
SMoveUp::homedMove_moveFailed ()
{
  if ((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))
    {
      this->p.out.moveFinished ();
      this->moveState = ::SMoveUp::MoveState::IDLE;
    }
  else if (!((this->homedState == ::SMoveUp::HomedState::NOT_HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
SMoveUp::regularMove_moveFinished ()
{
  if ((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))
    {
      this->p.out.moveFinished ();
      this->moveState = ::SMoveUp::MoveState::IDLE;
    }
  else if (!((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
SMoveUp::regularMove_moveAborted ()
{
  if ((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::ABORTING))
    {
      this->p.out.moveAborted ();
      this->moveState = ::SMoveUp::MoveState::IDLE;
    }
  else if (!((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::ABORTING))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
void
SMoveUp::regularMove_moveFailed ()
{
  if ((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))
    {
      this->p.out.moveFinished ();
      this->moveState = ::SMoveUp::MoveState::IDLE;
        {}
      this->homedState = ::SMoveUp::HomedState::NOT_HOMED;
    }
  else if (!((this->homedState == ::SMoveUp::HomedState::HOMED) && (this->moveState == ::SMoveUp::MoveState::MOVING))) this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
  else this->dzn_locator.get<dzn::illegal_handler> ().handle (LOCATION);
}
// version 2.18.2
